---
phase: 02-app-shell-deal-feed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - app/page.tsx
  - app/deals/page.tsx
  - app/deals/loading.tsx
  - app/globals.css
  - components/ui/badge.tsx
  - components/ui/card.tsx
  - components/ui/select.tsx
  - components/ui/button.tsx
  - lib/utils.ts
  - next.config.ts
  - tsconfig.json
  - postcss.config.mjs
  - biome.json
  - vercel.json
  - package.json
autonomous: true
requirements: [DEAL-01]

must_haves:
  truths:
    - "Next.js dev server starts successfully with `npx next dev` and renders the /deals page skeleton"
    - "Tailwind v4 utility classes render correctly (dark background, text colors visible)"
    - "shadcn/ui Badge and Card components are installed and importable"
    - "The existing daemon tsconfig.json is preserved unchanged — `npm run build` (tsc) still compiles daemon code"
    - "Vercel deployment succeeds with Next.js framework detection"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Root layout with dark theme, ThemeProvider, metadata"
      contains: "ThemeProvider"
    - path: "app/deals/page.tsx"
      provides: "Deals page placeholder (will be populated in Plan 02)"
      contains: "export default"
    - path: "app/globals.css"
      provides: "Tailwind v4 CSS with dark theme variables"
      contains: "@import"
    - path: "next.config.ts"
      provides: "Next.js config with default settings (no tsconfigPath needed — Next.js uses root tsconfig.json by default)"
      contains: "NextConfig"
    - path: "postcss.config.mjs"
      provides: "Tailwind v4 PostCSS plugin"
      contains: "@tailwindcss/postcss"
    - path: "biome.json"
      provides: "Biome linter/formatter config"
      contains: "biomejs.dev"
  key_links:
    - from: "package.json"
      to: "tsconfig.daemon.json"
      via: "build script with -p flag"
      pattern: "tsc -p tsconfig.daemon"
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "CSS import"
      pattern: "import.*globals"
---

<objective>
Scaffold the Next.js 16 app at repo root with App Router, Tailwind v4, shadcn/ui, Biome, and dark theme — then deploy to Vercel.

Purpose: Establish the frontend foundation that all subsequent Phase 2 and Phase 3+ plans build upon. The monorepo boundary between daemon (tsconfig.json) and Next.js (tsconfig.next.json) must be correctly configured so both coexist.

Note: Task 1 touches 16 files, which is above the normal threshold. This is expected — it is a scaffolding batch where most files are small config/boilerplate created from templates. The work is cohesive (all Next.js bootstrap). If context runs tight, Steps 11-12 (shadcn init + Biome init) can be deferred to a follow-up sub-task.

Output: A working Next.js app skeleton deployed to Vercel with dark theme, shadcn/ui components installed, and a placeholder /deals page.
</objective>

<execution_context>
@/Users/andeslee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andeslee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-app-shell-deal-feed/02-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs from existing codebase -->

From src/flights/db-drizzle.ts:
```typescript
export function getDrizzle(): ReturnType<typeof drizzle<typeof schema>>;
// Usage: const db = getDrizzle(); const rows = await db.execute(sql`...`);
```

From package.json (existing daemon scripts — must not break):
```json
{
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/flights/web-server.ts",
    "daemon": "tsx src/flights/flight-daemon.ts"
  }
}
```

From tsconfig.json (existing daemon config — must preserve):
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 app with Tailwind v4, shadcn/ui, Biome, and monorepo boundary</name>
  <files>
    app/layout.tsx
    app/page.tsx
    app/deals/page.tsx
    app/deals/loading.tsx
    app/globals.css
    components/ui/badge.tsx
    components/ui/card.tsx
    components/ui/select.tsx
    components/ui/button.tsx
    lib/utils.ts
    next.config.ts
    tsconfig.json (rename to tsconfig.daemon.json, create new tsconfig.json for Next.js)
    postcss.config.mjs
    biome.json
    package.json
    vercel.json
  </files>
  <action>
    **Step 1: Preserve daemon tsconfig**
    - Rename existing `tsconfig.json` → `tsconfig.daemon.json`
    - Update `package.json` script `"build"` from `"tsc"` to `"tsc -p tsconfig.daemon.json"` so daemon compilation still works
    - Update `drizzle/drizzle.config.ts` if it references tsconfig.json (check first)

    **Step 2: Install Next.js and dependencies**
    Do NOT use `create-next-app` (it would overwrite existing files). Install manually:
    ```bash
    npm install next@latest react@latest react-dom@latest next-themes zustand
    npm install -D @tailwindcss/postcss @biomejs/biome @types/react @types/react-dom
    npm install tailwindcss postcss tw-animate-css
    ```

    **Step 3: Create tsconfig.json for Next.js**
    Create a new `tsconfig.json` at root (Next.js requires this exact name):
    ```json
    {
      "compilerOptions": {
        "target": "ES2022",
        "lib": ["dom", "dom.iterable", "esnext"],
        "allowJs": true,
        "skipLibCheck": true,
        "strict": true,
        "noEmit": true,
        "esModuleInterop": true,
        "module": "esnext",
        "moduleResolution": "bundler",
        "resolveJsonModule": true,
        "isolatedModules": true,
        "jsx": "preserve",
        "incremental": true,
        "plugins": [{ "name": "next" }],
        "paths": { "@/*": ["./*"] }
      },
      "include": [
        "next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts",
        "app/**/*", "components/**/*", "lib/**/*", "stores/**/*",
        "src/flights/db-drizzle.ts", "src/flights/db-schema.ts",
        "src/flights/sweet-spots.ts", "src/flights/transfer-partners.ts",
        "src/flights/airports.ts", "src/flights/types.ts", "src/flights/monitor.ts"
      ],
      "exclude": ["node_modules", "dist", ".next"]
    }
    ```

    **Step 4: Create next.config.ts**
    ```typescript
    import type { NextConfig } from 'next';
    const config: NextConfig = {};
    export default config;
    ```
    Note: Do NOT set `typescript.tsconfigPath` — Next.js uses `tsconfig.json` at root by default, which is now the Next.js tsconfig. The daemon uses `tsconfig.daemon.json` via the `-p` flag.

    **Step 5: Create postcss.config.mjs**
    ```javascript
    const config = {
      plugins: {
        '@tailwindcss/postcss': {},
      },
    };
    export default config;
    ```

    **Step 6: Create app/globals.css with Tailwind v4 dark theme**
    ```css
    @import "tailwindcss";
    @import "tw-animate-css";

    @custom-variant dark (&:is(.dark *));

    @theme inline {
      --radius: 0.625rem;
      --color-background: oklch(0.145 0 0);
      --color-foreground: oklch(0.985 0 0);
      --color-card: oklch(0.205 0 0);
      --color-card-foreground: oklch(0.985 0 0);
      --color-border: oklch(0.269 0 0);
      --color-input: oklch(0.269 0 0);
      --color-ring: oklch(0.556 0 0);
      --color-primary: oklch(0.769 0.188 70.08);
      --color-primary-foreground: oklch(0.145 0 0);
      --color-secondary: oklch(0.269 0 0);
      --color-secondary-foreground: oklch(0.985 0 0);
      --color-muted: oklch(0.205 0 0);
      --color-muted-foreground: oklch(0.556 0 0);
      --color-accent: oklch(0.269 0 0);
      --color-accent-foreground: oklch(0.985 0 0);
      --color-destructive: oklch(0.577 0.245 27.325);
      --color-destructive-foreground: oklch(0.985 0 0);
      --color-popover: oklch(0.205 0 0);
      --color-popover-foreground: oklch(0.985 0 0);
    }

    body {
      background-color: var(--color-background);
      color: var(--color-foreground);
    }
    ```

    **Step 7: Create app/layout.tsx (root layout with dark theme)**
    ```tsx
    import type { Metadata } from 'next';
    import { ThemeProvider } from 'next-themes';
    import './globals.css';

    export const metadata: Metadata = {
      title: 'Flight Points - Award Flight Deals',
      description: 'Find the best award flight deals across all loyalty programs',
    };

    export default function RootLayout({ children }: { children: React.ReactNode }) {
      return (
        <html lang="en" suppressHydrationWarning>
          <body className="min-h-screen antialiased">
            <ThemeProvider attribute="class" defaultTheme="dark" enableSystem={false}>
              {children}
            </ThemeProvider>
          </body>
        </html>
      );
    }
    ```

    **Step 8: Create app/page.tsx (redirect to /deals)**
    ```tsx
    import { redirect } from 'next/navigation';
    export default function Home() {
      redirect('/deals');
    }
    ```

    **Step 9: Create app/deals/page.tsx (placeholder)**
    ```tsx
    export default function DealsPage() {
      return (
        <main className="container mx-auto px-4 py-8">
          <h1 className="text-3xl font-bold mb-2">Award Deal Feed</h1>
          <p className="text-muted-foreground mb-6">
            Best current award flight deals across all programs
          </p>
          <div className="text-muted-foreground">Loading deals...</div>
        </main>
      );
    }
    ```

    **Step 10: Create app/deals/loading.tsx**
    ```tsx
    export default function Loading() {
      return (
        <main className="container mx-auto px-4 py-8">
          <div className="h-8 w-48 bg-muted animate-pulse rounded mb-6" />
          <div className="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-4">
            {Array.from({ length: 6 }).map((_, i) => (
              <div key={i} className="h-48 bg-muted animate-pulse rounded-lg" />
            ))}
          </div>
        </main>
      );
    }
    ```

    **Step 11: Initialize shadcn/ui**
    Run `npx shadcn@latest init` — when prompted:
    - Style: new-york
    - Base color: zinc
    - CSS variables: yes

    If interactive prompts are a problem, create `components.json` manually:
    ```json
    {
      "$schema": "https://ui.shadcn.com/schema.json",
      "style": "new-york",
      "rsc": true,
      "tsx": true,
      "tailwind": {
        "config": "",
        "css": "app/globals.css",
        "baseColor": "zinc",
        "cssVariables": true
      },
      "aliases": {
        "components": "@/components",
        "utils": "@/lib/utils",
        "ui": "@/components/ui",
        "lib": "@/lib",
        "hooks": "@/hooks"
      },
      "iconLibrary": "lucide"
    }
    ```

    Then add components: `npx shadcn@latest add badge card select button`

    If shadcn init fails or creates wrong Tailwind config, ensure `lib/utils.ts` has the `cn()` helper:
    ```typescript
    import { clsx, type ClassValue } from 'clsx';
    import { twMerge } from 'tailwind-merge';
    export function cn(...inputs: ClassValue[]) {
      return twMerge(clsx(inputs));
    }
    ```
    And install: `npm install clsx tailwind-merge`

    **Step 12: Initialize Biome**
    Run `npx @biomejs/biome init` to create `biome.json`, then update it:
    ```json
    {
      "$schema": "https://biomejs.dev/schemas/2.0.0/schema.json",
      "vcs": { "enabled": true, "clientKind": "git", "useIgnoreFile": true },
      "files": {
        "include": ["app/**/*.ts", "app/**/*.tsx", "components/**/*.ts",
                     "components/**/*.tsx", "lib/**/*.ts", "stores/**/*.ts"]
      },
      "formatter": {
        "enabled": true, "indentStyle": "space", "indentWidth": 2, "lineWidth": 100
      },
      "linter": {
        "enabled": true,
        "rules": { "recommended": true }
      },
      "javascript": {
        "formatter": { "quoteStyle": "single", "trailingCommas": "all", "semicolons": "always" }
      }
    }
    ```

    **Step 13: Update vercel.json for Next.js**
    Replace the existing static-file vercel.json with:
    ```json
    {
      "framework": "nextjs"
    }
    ```
    Remove the old `buildCommand`, `rewrites`, and `functions` entries. Next.js handles all routing via App Router.

    **Step 14: Update package.json scripts**
    - Remove or rename `"vercel-build"` script (no longer needed — Next.js auto-builds on Vercel)
    - Add `"next-dev": "next dev"` and `"next-build": "next build"` scripts
    - Keep existing daemon scripts (`dev`, `daemon`, `search`, `build`) working

    **Step 15: Add .next to .gitignore**
    Append `.next` and `.next/` to `.gitignore` if not already present.

    **Step 16: Verify daemon still compiles**
    Run `npx tsc -p tsconfig.daemon.json --noEmit` to confirm the daemon code still type-checks correctly.

    **Step 17: Verify Next.js dev server starts**
    Run `npx next build` (or `npx next dev` briefly) to confirm the app compiles and renders.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.daemon.json --noEmit 2>&1 | tail -5 && npx next build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `npx next dev` starts and renders /deals with dark background and "Award Deal Feed" heading
    - `npx tsc -p tsconfig.daemon.json --noEmit` succeeds (daemon code still compiles)
    - shadcn/ui Badge, Card, Select, Button components exist in components/ui/
    - Tailwind v4 classes render correctly (dark theme visible)
    - biome.json exists with linting enabled
    - postcss.config.mjs uses @tailwindcss/postcss
  </done>
</task>

<task type="auto">
  <name>Task 2: Deploy Next.js app to Vercel and verify</name>
  <files>vercel.json</files>
  <action>
    **Deploy to Vercel:**
    The project is already connected to Vercel via GitHub (andeslee444/Flight-Points). Pushing to main will trigger an automatic deployment.

    However, verify the following before pushing:
    1. `vercel.json` has `"framework": "nextjs"` (from Task 1)
    2. `DATABASE_URL` env var is set in Vercel dashboard (should already be from Phase 1)
    3. No Python/daemon imports leak into the Next.js app/ directory

    After the commit lands on main, Vercel will auto-deploy. If Vercel CLI is available, you can also run:
    ```bash
    npx vercel --yes
    ```
    to trigger a preview deployment for verification.

    **Important:** The existing Vercel deployment was serving static HTML from `web/public/`. The new deployment will serve the Next.js app instead. The old static files (`flights.html`, `flight-results.html`) are still accessible on Harbor's Express server (port 3000) but will no longer be served by Vercel.

    If the Vercel build fails, check:
    - Missing dependencies (shadcn/ui components need clsx, tailwind-merge, @radix-ui/*)
    - TypeScript errors from Next.js tsconfig including daemon files it shouldn't
    - The tsconfig.json `include` array being too broad
  </action>
  <verify>
    <automated>npx next build 2>&1 | grep -E "Route|Generating|✓|error" | head -20</automated>
  </verify>
  <done>
    - `npx next build` succeeds locally
    - Vercel deployment succeeds (after git push or vercel CLI deploy)
    - Visiting the Vercel URL shows the dark-themed /deals placeholder page
  </done>
</task>

</tasks>

<verification>
1. `npx next dev` starts on port 3000 (or 3001 if 3000 is taken) and shows dark-themed /deals page
2. `npx tsc -p tsconfig.daemon.json --noEmit` passes — daemon code unaffected
3. shadcn/ui components render (Badge, Card visible in dev tools)
4. `npx next build` produces no errors
5. Vercel deployment shows the Next.js app (not old static HTML)
</verification>

<success_criteria>
- Next.js 16 app running at repo root with App Router, Tailwind v4, shadcn/ui, dark theme
- Daemon TypeScript compilation preserved via tsconfig.daemon.json
- Vercel deployment succeeds with Next.js framework detection
- /deals page renders with dark background and placeholder text
</success_criteria>

<output>
After completion, create `.planning/phases/02-app-shell-deal-feed/02-01-SUMMARY.md`
</output>
